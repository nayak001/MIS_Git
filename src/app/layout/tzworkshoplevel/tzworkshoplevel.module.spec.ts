import { TzworkshoplevelModule } from './tzworkshoplevel.module';

describe('TzworkshoplevelModule', () => {
  let tzworkshoplevelModule: TzworkshoplevelModule;

  beforeEach(() => {
    tzworkshoplevelModule = new TzworkshoplevelModule();
  });

  it('should create an instance', () => {
    expect(tzworkshoplevelModule).toBeTruthy();
  });
});
