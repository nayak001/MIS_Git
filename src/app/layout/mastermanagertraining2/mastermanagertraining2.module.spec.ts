import { Mastermanagertraining2Module } from './mastermanagertraining2.module';

describe('Mastermanagertraining2Module', () => {
  let mastermanagertraining2Module: Mastermanagertraining2Module;

  beforeEach(() => {
    mastermanagertraining2Module = new Mastermanagertraining2Module();
  });

  it('should create an instance', () => {
    expect(mastermanagertraining2Module).toBeTruthy();
  });
});
